id: test-custom-arguments-pack
kind: ModelPackaging
spec:
  artifactName: "simple-model.zip"
  targets:
    - connectionName: docker-ci
      name: docker-push
  arguments:
    dockerfileBaseImage: True
    timeout: "not allowed argument"
    dockerfileAddCondaInstallation: "not allowed argument"
  integrationName: docker-cli

